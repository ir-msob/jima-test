import {BaseModel} from "../base-model";
import {BaseType} from "./base-type";

/**
 * Class ModelType.
 * It implements the BaseModel and BaseType interfaces.
 * @class
 * @implements {BaseModel}
 * @implements {BaseType}
 */
export class ModelType implements BaseModel, BaseType {
}
